# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
# after-startup-command = ['exec-and-forget sketchybar']
after-startup-command = []

# Notify Sketchybar about workspace change
# exec-on-workspace-change = ['/bin/bash', '-c',
#     'sketchybar --trigger aerospace_workspace_change AEROSPACE_FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE AEROSPACE_PREV_WORKSPACE=$AEROSPACE_PREV_WORKSPACE'
# ]

exec-on-workspace-change = []

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 20

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
# on-focus-changed = ['move-mouse window-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = true 

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 12
inner.vertical = 12
outer.left = 12
outer.bottom = 12
outer.top = 12
outer.right = 12

[workspace-to-monitor-force-assignment]
# 1 = ["BenQ PD2500Q"]
# 2 = ["LS27A600U"]
# 3 = ["Built-in Retina Display"]
# 4 = ["Built-in Retina Display"]
# w = ["BenQ GL2580"]
# s = ["BenQ PD2500Q"]

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]
#cmd-h = []     # Disable "hide application"
cmd-alt-h = [] # Disable "hide others"
# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
#                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
#                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
#                   sectionSign (ISO keyboards only, european keyboards only)
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# You can uncomment the following lines to open up terminal with alt + enter shortcut
# (like in i3)
# alt-enter = '''exec-and-forget osascript -e '
# tell application "Terminal"
#     do script
#     activate
# end tell'
# '''

# alt-enter = "exec-and-forget kitty --single-instance -d ~"
# alt-shift-enter = "exec-and-forget ~/.config/aerospace/kitty-finder.sh"

# alt-ctrl-cmd-n = "exec-and-forget ~/.config/aerospace/ghostty-quick-note.sh"

alt-ctrl-cmd-shift-h = ['join-with left', 'mode main']
alt-ctrl-cmd-shift-j = ['join-with down', 'mode main']
alt-ctrl-cmd-shift-k = ['join-with up', 'mode main']
alt-ctrl-cmd-shift-l = ['join-with right', 'mode main']

alt-ctrl-cmd-left = "focus-monitor --wrap-around left"
alt-ctrl-cmd-down = "focus-monitor --wrap-around down"
alt-ctrl-cmd-up = "focus-monitor --wrap-around up"
alt-ctrl-cmd-right = "focus-monitor --wrap-around right"

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
alt-ctrl-cmd-h = 'focus left --boundaries-action wrap-around-the-workspace'
alt-ctrl-cmd-j = 'focus down --boundaries-action wrap-around-the-workspace'
alt-ctrl-cmd-k = 'focus up --boundaries-action wrap-around-the-workspace'
alt-ctrl-cmd-l = 'focus right --boundaries-action wrap-around-the-workspace'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
alt-ctrl-cmd-minus = 'resize smart -50'
alt-ctrl-cmd-equal = 'resize smart +50'

alt-ctrl-cmd-b = ['workspace-back-and-forth']
alt-ctrl-cmd-1 = ['workspace 1']
alt-ctrl-cmd-2 = ['workspace 2']
alt-ctrl-cmd-3 = ['workspace 3']
alt-ctrl-cmd-4 = ['workspace 4']
alt-ctrl-cmd-5 = ['workspace 5']
alt-ctrl-cmd-6 = ['workspace 6']
alt-ctrl-cmd-7 = ['workspace 7']
alt-ctrl-cmd-8 = ['workspace 8']
alt-ctrl-cmd-9 = ['workspace 9']
alt-ctrl-cmd-0 = ['workspace 0']
alt-ctrl-cmd-f = ['workspace f']
alt-ctrl-cmd-m = ['workspace memo']

alt-ctrl-cmd-shift-tab = ['move-workspace-to-monitor --wrap-around next']

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-ctrl-cmd-a = 'mode service'
# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']

backspace = ['close-all-windows-but-current', 'mode main']

shift-1 = ['move-node-to-workspace 1 --focus-follows-window', 'mode main']
shift-2 = ['move-node-to-workspace 2 --focus-follows-window', 'mode main']
shift-3 = ['move-node-to-workspace 3 --focus-follows-window', 'mode main']
shift-4 = ['move-node-to-workspace 4 --focus-follows-window', 'mode main']
shift-5 = ['move-node-to-workspace 5 --focus-follows-window', 'mode main']
shift-6 = ['move-node-to-workspace 6 --focus-follows-window', 'mode main']
shift-7 = ['move-node-to-workspace 7 --focus-follows-window', 'mode main']
shift-8 = ['move-node-to-workspace 8 --focus-follows-window', 'mode main']
shift-9 = ['move-node-to-workspace 9 --focus-follows-window', 'mode main']
shift-0 = ['move-node-to-workspace 0 --focus-follows-window', 'mode main']
shift-f = ['move-node-to-workspace f --focus-follows-window', 'mode main']
alt-ctrl-cmd-m = ['move-node-to-workspace memo --focus-follows-window', 'mode main']


# sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
#s = ['layout sticky tiling', 'mode main']

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
# z = ["exec-and-forget brew services restart sketchybar", 'mode main']

# See: https://nikitabobko.github.io/AeroSpace/commands#move
shift-h = ['move left', 'mode main']
shift-j = ['move down', 'mode main']
shift-k = ['move up', 'mode main']
shift-l = ['move right', 'mode main']

v = ['layout vertical', 'mode main']
s = ['layout horizontal', 'mode main']
z = ["fullscreen", 'mode main']
a = ['layout accordion tiles', 'mode main']
f5 = ['flatten-workspace-tree', 'mode main']                # reset layout
f = [
	'layout floating tiling',
	'mode main',
] # Toggle between floating and tiling layout

shift-left = ['move-workspace-to-monitor left --wrap-around', 'mode main']
shift-down = ['move-workspace-to-monitor down --wrap-around', 'mode main']
shift-up = ['move-workspace-to-monitor up --wrap-around', 'mode main']
shift-right = ['move-workspace-to-monitor right --wrap-around', 'mode main']

[[on-window-detected]]
  if.workspace = '9'
  check-further-callbacks = true
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "company.thebrowser.dia"
  run = ['move-node-to-workspace 2']

[[on-window-detected]]
  if.workspace = '0'
  check-further-callbacks = true
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.microsoft.autoupdate2"
  run = ['layout floating', 'move-node-to-workspace 9']

[[on-window-detected]]
  if.app-id = "com.bitwarden.desktop"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.apple.script.id.MacYTDL"
  run = ['layout floating']

[[on-window-detected]]
  if.app-name-regex-substring = "osascript"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.TickTick.task.mac"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.apple.calculator"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.apple.AppStore"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.apple.Notes"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.apple.iCal"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.apple.finder"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.apple.UserNotificationCenter"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.fortinet.forticlient.macos.vpn"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.fortinet.FortiClient"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "cc.ffitch.shottr"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "net.kovidgoyal.kitty-quick-access"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.apple.Preview"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.anthropic.claudefordesktop"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.apple.accessibility.universalAccessAuthWarn"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.goodsnooze.MacWhisper"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.bitwarden.desktop"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.google.chrome.for.testing"
  run = ["layout floating"]

[[on-window-detected]]
  if.app-id = "org.videolan.vlc"
  run = ['move-node-to-workspace 9']

[[on-window-detected]]
  if.app-id = "com.spotify.client"
  run = ['move-node-to-workspace 9']

[[on-window-detected]]
  if.app-id = "com.hnc.Discord"
  run = ['move-node-to-workspace 9']

[[on-window-detected]]
  if.app-id = "com.figma.Desktop"
  run = ['move-node-to-workspace f']

[[on-window-detected]]
  if.app-id = "com.apple.Safari"
  run = ['move-node-to-workspace s']

[[on-window-detected]]
  if.app-id = "com.codeweavers.CrossOver"
  run = ['move-node-to-workspace 0']

[[on-window-detected]]
  if.app-id = "com.valvesoftware.steam"
  run = ['move-node-to-workspace 0']

[[on-window-detected]]
  if.app-id = "com.valvesoftware.steam.helper"
  run = ['move-node-to-workspace 0']

[[on-window-detected]]
  if.app-id = "com.electron.trilium-notes"
  run = ["move-node-to-workspace memo"]

[[on-window-detected]]
  if.app-id = "com.apple.ScreenSharing"
  run = ['layout floating']

[[on-window-detected]]
  if.app-id = "com.gizmolabs.openwhispr"
  run = ["layout floating"]
